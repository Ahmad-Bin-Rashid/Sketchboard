/**
 * Whiteboard components barrel export.
 *
 * Import from "@/components/whiteboard" instead of individual files.
 *
 * Components:
 * - Whiteboard:          Main canvas wrapper (Phase 2+)
 * - BoardHeader:         Floating top bar (Phase 2+)
 * - ConnectionIndicator: Auto-hiding status pill (Phase 3)
 * - RemoteCursors:       Live cursor overlay (Phase 4)
 * - CursorAvatar:        Single remote cursor (Phase 4)
 * - ActiveUsersPanel:    Stacked collaborator avatars (Phase 4)
 */

export { Whiteboard } from "./whiteboard";
export { BoardHeader } from "./board-header";
export { ConnectionIndicator } from "./connection-indicator";
export { RemoteCursors } from "./remote-cursors";
export { CursorAvatar } from "./cursor-avatar";
export { ActiveUsersPanel } from "./active-users-panel";
