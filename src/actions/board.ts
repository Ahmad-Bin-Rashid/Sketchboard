/**
 * Board server actions barrel export.
 *
 * Server Actions are colocated here by domain.
 * They handle DB mutations with auth checks and return ActionResult types.
 *
 * Usage in client components:
 *   import { createBoard, deleteBoard } from "@/actions/board";
 */

// Will be fully implemented in Phase 7 (Dashboard & Board Management)
